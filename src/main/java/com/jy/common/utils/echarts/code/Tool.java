package com.jy.common.utils.echarts.code;

public enum Tool
{
    dataView("dataView", 0), 
    dataZoom("dataZoom", 1), 
    magicType("magicType", 2), 
    mark("mark", 3), 
    restore("restore", 4), 
    saveAsImage("saveAsImage", 5);
    
    private Tool(final String s, final int n) {
    }
}
