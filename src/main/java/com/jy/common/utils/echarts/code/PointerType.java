package com.jy.common.utils.echarts.code;

public enum PointerType
{
    line("line", 0), 
    cross("cross", 1), 
    shadow("shadow", 2), 
    none("none", 3);
    
    private PointerType(final String s, final int n) {
    }
}
