package com.jy.common.utils.echarts.code;

public enum BrushType
{
    both("both", 0), 
    stroke("stroke", 1), 
    fill("fill", 2);
    
    private BrushType(final String s, final int n) {
    }
}
