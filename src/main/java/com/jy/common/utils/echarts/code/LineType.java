package com.jy.common.utils.echarts.code;

public enum LineType
{
    solid("solid", 0), 
    dotted("dotted", 1), 
    dashed("dashed", 2), 
    broken("broken", 3), 
    curve("curve", 4);
    
    private LineType(final String s, final int n) {
    }
}
