package com.jy.common.utils.echarts.code;

public enum Baseline
{
    top("top", 0), 
    bottom("bottom", 1), 
    middle("middle", 2);
    
    private Baseline(final String s, final int n) {
    }
}
