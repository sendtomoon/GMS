package com.jy.common.utils.echarts.code;

public enum AxisType
{
    category("category", 0), 
    value("value", 1), 
    time("time", 2), 
    log("log", 3);
    
    private AxisType(final String s, final int n) {
    }
}
