package com.jy.common.utils.echarts.code;

public enum Sort
{
    none("none", 0), 
    ascending("ascending", 1), 
    descending("descending", 2);
    
    private Sort(final String s, final int n) {
    }
}
